tosca_definitions_version: nativeedge_1_0

imports:
  - nativeedge/types/types.yaml
  - plugin:nativeedge-azure-plugin

labels:

  hidden:
    values:
      - false

  csys-obj-parent:
    values:
      - foo
