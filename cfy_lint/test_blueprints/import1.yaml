tosca_definitions_version: cloudify_dsl_1_4

node_types:

  cloudify.nodes.types.Used:
    derived_from: cloudify.nodes.Root

    
node_templates:
  pod:
    type: cloudify.nodes.kubernetes.resources.Pod
